{
  "name": "m365-aitm-guard",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension that blocks AiTM phishing proxies targeting Microsoft 365.",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "typescript": "^5.6.3"
  }
}

